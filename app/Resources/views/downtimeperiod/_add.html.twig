{% block body %}
    <h1>Downtimeperiod creation</h1>

    {{ game.name }}
    {{ game.id }}

    {{ form_start(form, {method: 'POST', action: path('downtimeperiod_new', {'game_id': game.id})}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('downtimeperiod_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
